# Unity-generated folders and files — never commit these.
# Scoped to the unity/ tree only; does not affect the repo root .gitignore.
Library/
Temp/
Obj/
Logs/
UserSettings/
Builds/
Build/
.vs/

# Generated solution/project files
*.csproj
*.sln
*.suo
*.user
*.userprefs

# Unity crash/build logs
*.stackdump

# Obsolete leftover default Unity project (untracked in PR #189).
# It has no Assets/ — only generated caches and stock settings. The real
# project is Penalty444Client/. Ignored so it never returns and doesn't
# show as noise on machines that still have the folder locally.
New Unity Project/
